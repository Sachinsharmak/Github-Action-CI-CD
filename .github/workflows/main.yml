name: GitHub Actions
on: workflow_dispatch
jobs: 
  Testing: 
    runs-on: ubuntu-latest
    steps: 
      - name: Code Checkout
        uses: actions/checkout@v4
      
      - name: Maven Test
        run: mvn test

      - name: Checskstyle
        run: mvn checkstyle:check